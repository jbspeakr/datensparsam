language: python
python:
  - "3.4"
install:
  - pip install -q -r requirements.txt --use-mirrors
  - pip install coveralls
script:
  coverage run --source=datensparsam setup.py -q test
after_success:
  coveralls
